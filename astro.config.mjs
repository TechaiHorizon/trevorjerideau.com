// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	site: 'https://trevorjerideau.github.io',
	// Remove the base if deploying to a custom domain
	// base: '/trevorjerideau.com',
});
